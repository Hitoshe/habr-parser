version: '3.8'
services:
  db:
    image: postgres:15
    container_name: habr_db_container
    environment:
      POSTGRES_DB: habr_storage
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5435:5432"